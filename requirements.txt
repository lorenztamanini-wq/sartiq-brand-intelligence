# --- Offline core (required; pure-Python, runs with no API key / no network) ---
pydantic>=2.0          # schema + validation (the deterministic contract)
PyYAML>=6.0            # benchmarks + ground-truth data files
Jinja2>=3.1            # brief rendering (markdown / HTML templates)

# --- Live digging (optional; only used when ANTHROPIC_API_KEY is set) ---
anthropic>=0.40        # orchestration, vision classification, synthesis
httpx>=0.27            # sitemaps, JSON-LD, wayback CDX, people API
beautifulsoup4>=4.12   # HTML parsing fallback
lxml>=5.0             # fast XML/HTML (sitemaps)
ultimate-sitemap-parser>=1.8  # robust catalog enumeration: gzip + sitemap-index + robots.txt (live-only)

# --- Human-in-the-loop review UI (needed for `streamlit run app.py`) ---
streamlit>=1.30        # the review form — confirm/sharpen flagged fields, then publish

# --- Optional extras (degrade gracefully if absent) ---
# playwright>=1.40     # JS-heavy PDP rendering (falls back to httpx + JSON-LD)
# weasyprint>=60       # brief -> PDF (always emits markdown + HTML regardless)
# gspread>=6.0         # push the prioritization sheet to Google Sheets (else local CSV/MD)

# Tests run on the stdlib `unittest` (no pytest dependency required).
